import 'package:flutter/material.dart';

class MainFeedPage extends StatelessWidget {
  const MainFeedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Main Feed Page'),
      ),
    );
  }
}
